{
  "name": "maim",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm test && npm run build",
    "dev": "npm run build && python3 -m http.server 8791 --bind 127.0.0.1 --directory dist"
  }
}
